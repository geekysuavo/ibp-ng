
/* ensure once-only inclusion. */
#pragma once

#include <stdio.h>
#include <stdlib.h>

/* include the cuda headers. */
#ifdef __IBP_HAVE_CUDA
#include <cuda.h>
#include <cuda_runtime.h>
#endif

/* function declarations (enum-gpu.cu): */

/* FIXME: functions for enum-gpu.cu ?? */

